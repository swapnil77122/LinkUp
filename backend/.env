PORT=4000
PGHOST=localhost
PGUSER=postgres
PGPASSWORD=oracle
PGDATABASE=messaging_app
PGPORT=5432
OPENAI_API_KEY=                 # optional; leave empty if not using OpenAI
AI_ENABLED=false                # set to true to enable OpenAI calls (if key set)
